# The activity, the tile service, the watch service and the boot receiver are
# only referenced from the manifest, which AGP already turns into keep rules.
# Flutter ships its own consumer rules for the engine, so nothing is needed here.
